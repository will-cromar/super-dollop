/* Test Case 1 contains no errors. */
var x, y, z, z1;
begin
    x := 2;
    x := x + 2; x := x + 3;

    y := x;
    y := y + 1;
    y := x / y;

    x := x * x;
    if x >= y then
        x := y;

    if (x+2) < y then
        z := x+2;
        z1 := z + 1;
end.